plugins {
  id 'jetty'
}

apply plugin: 'war'
war {
	archiveName 'Priam.war'
}

dependencies {
    compile project(':priam')
    compile project(':priam-dse-extensions')
    compile project(':priam-cass-extensions')
}
